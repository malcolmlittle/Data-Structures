# import Array module because python doesn't have them built in
from array import *

array1 = array('i', [1, 2, 3, 4, 5, 6])
array2 = array('d', [1.3, 1.5, 1.6])

print(array1)

# creating an array is an O(1) constant time operation